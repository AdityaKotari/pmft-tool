# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
/tsconfig.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (.env holds the user's real EDGAR identity — never commit;
# .env.example is the committed template)
.env
.env.local
.env.*.local

# data (DB files, not committed — user generates them)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.csv
data/*.parquet
data/.backfill-pid
data/.backfill-log

# Python pipeline
pipeline/__pycache__/
pipeline/.venv/
pipeline/.mypy_cache/
pipeline/.pytest_cache/
pipeline/.ruff_cache/
pipeline/*.egg-info/
pipeline/uv.lock
__pycache__/
